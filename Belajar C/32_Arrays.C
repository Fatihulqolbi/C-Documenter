#include <stdio.h>

int main (){
// Array = a data structure that cam store many values of the same data type.

double price[10] = {5.0, 10.0, 15.0, 20.0, 25.0};


printf("%.1lf", price[0]);
  

  return 0;
}