#include <stdio.h>

int main( ){
  // Arithmetic operator

  // + (addition)
  // - (substraction)
  // * (multiplication)
  // / (division)
  // % (modulus)
  // ++ (Increment)
  // -- (decrement)

  int x = 5;
  int y = 2;
  // int z = x + y;
  // int z = x - y;
  // int z = x * y;

  // float z = x / (float) y;
  int z = x % y;
  // int x = 5;
  // int y = 2;
  // x++;
  // y--;
  printf("%d", z);

  return 0;

}